# Runtime dependencies for Synaptipy
# Test dependencies are in pyproject.toml under [project.optional-dependencies] dev
# Install via: pip install -e ".[dev]"

# Core scientific stack
numpy>=2.0.0,<3.0.0
scipy>=1.14.0,<2.0.0
pandas>=2.3.0
matplotlib>=3.8.0,<4.0.0

# GUI
pyqtgraph>=0.13.3,<1.0.0
# PySide6: pin to 6.7.3 on Windows (avoids QTBUG-130070 crash in 6.8.0).
# On all other platforms allow >=6.7.0 excluding the known-bad 6.8.0.
PySide6==6.7.3 ; sys_platform == 'win32'
PySide6>=6.7.0, !=6.8.0, <7.0.0 ; sys_platform != 'win32'

# Data format support
neo>=0.14.0,<1.0.0
pynwb>=3.1.0
h5py>=3.14.0
hdmf>=4.1.0
quantities>=0.16.1

# Schema/validation
jsonschema>=4.25.0
jsonschema-specifications>=2025.4.0
referencing>=0.36.0
rpds-py>=0.27.0

# YAML
ruamel-yaml>=0.18.0
ruamel-yaml-clib>=0.2.0

# Utilities
attrs>=25.3.0
packaging>=25.0
platformdirs>=4.3.0
python-dateutil>=2.9.0
pytz>=2025.0
six>=1.17.0
typing-extensions>=4.14.0
tzdata>=2025.0
tzlocal>=5.3.0
